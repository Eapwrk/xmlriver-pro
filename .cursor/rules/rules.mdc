---
alwaysApply: true
---
🔍 Поиск решений:
Если не уверен на 100% или столкнулся с проблемой то используй инструмент context7 для поиска решения по лучшим практикам
❗ Всегда сначала используй context7 для поиска решений по библиотекам и лучшим практикам, только если context7 недоступен - сообщи об этом и используй web_search как запасной вариант.
Учитывай языковой и региональный контекст проблемы в рамках которой ты ищешь решения в плане языка, иногда лучше задать вопросы на русском если контекст проблемы находится в области RU специфичных инструментов, сервисов, библиотек и т.д., но первично ищи на EN если не дало результатов и контекст специфичен для RU - ищи на RU
Если тебе нужно исследовать репозиторий на Github обязательно используй для этого инструмент gitmcp-docs

---
📜 Принципы:
Соблюдать стандарты: Zen of Python, PEP 8, SOLID, DRY, KISS и принципы «Чистого кода» Роберта Мартина
Всегда проверяй что создан и активирован .venv если его нет или он не активен - создай и активируй
Всегда устанавливай последние библиотеки через pip в .venv, обновляй сам pip если нужно
Храни чувствительные данные в .env используй для доступа python-dotenv
Поддерживай файловый порядок и файловую гигиену, не плоди зоопарк документации (аудитов, отчетов, инструкций и т.п.), файлов для теста, отчетов и т.д., удаляй временные файлы которые создаешь для проверки и не планируется переиспользовать и т.д.
Соблюдай качественную структуру проекта, тесты в /test логи в /log документы в /doc и т.д.! соблюдай порядок!

🚫 ЗАПРЕТ НА СОЗДАНИЕ ЛИШНИХ ИНСТРУКЦИЙ:
НИКОГДА не создавай множество инструкций/README/SETUP/GUIDE файлов для одной задачи!
МАКСИМУМ 1 файл инструкции на задачу, если вообще нужен
Вся информация должна быть в существующих файлах проекта (README.md, docs/, rules.mdc)
Если создал временный файл для объяснения - УДАЛИ его сразу после завершения задачи
Примеры ЗАПРЕЩЕННЫХ действий:
❌ SETUP_SOMETHING.md, QUICK_START.md, FINAL_GUIDE.md для одной настройки
❌ test_temp.py, check_this.py и другие временные тестовые файлы
❌ report_1.txt, audit_new.md и прочие отчеты/аудиты
✅ Используй существующие: README.md, docs/*.md, .cursor/rules/rules.mdc
Будь педантичным и внимательным
Будь честным и открытым
Всегда отвечай на русском языке
Веди разработку на русском языке, все документы, докстринги и т.д. - на русском, если пользователь явно не указал другого
Я твой руководитель, а не программист, если есть вопросы по логике - объясняй какое решение к чему приведет
Излагать мысли кратко и лаконично
Не использовать эмодзи в коде предназначенном для вывода в консоль (в UI, экспорте и т.д. - можно, но умеренно, там где это действительно нужно для акцента и будет уместно)
Избегать использования хардкода! Все решения должны быть по возможности универсальными, без хардкода! Если хардкод неизбежен, останови выполнение задачи и предупреди пользователя об этом вместе с эмоджи - 🔒

---
💻 Команды:
По команде !линт запускай цикл Black -> Pylint -> MyPy через read_lints() и CLI
Линтеры интегрированы: Black форматирует при сохранении, Pylint и Mypy проверяют автоматически через расширения
Если нужно вручную: python -m black . && python -m pylint xmlriver_pro && python -m mypy xmlriver_pro
По команде !тест пиши pytest и тестируй весь проект
По команде !оптимизация - проверяй решешение на оптимизацию производительности, полный цикл - пиши тесты, ищи бутылочные горлышки и т.д.
По команде !самодиагностика - объяснения в одну строку каждой проблемы с которой ты столкнулся при последней задаче для копирования в инструкции, чтобы тебе было понятно решение если ты вновь сталкнешься с подобными проблемами, объясняй таким образом только проблемы которые могут быть универсальными для любого проекта, остальные игнорируй
По команде !мусор - исследуй проект на лишние не используемые файлы и удали их, наведи 🗑️ порядок
По команде !релиз - автоматически определяешь следующую версию → обновляешь CHANGELOG.md → запускаешь update_version.py → проверяешь git/тесты/линтинг → коммитишь и пушиш → создаешь тег → пушаешь тег → собираешь пакет python -m build → загружаешь на PyPI через twine upload → GitHub Actions создает GitHub Release (пользователь ничего не делает)
По команде !контекст - немедленно запускаешь поиск решений и дополнительных исследований по последним проблемам или вопросам через инструмент context7

---
🔍 Линтинг и проверка кода:
Линтеры работают автоматически через расширения Cursor:
- **Cursor Pyright** (встроенный) - проверка типов в реальном времени
- **Pylint** (ms-python.pylint) - проверка качества кода при открытии/сохранении файлов
- **Mypy Type Checker** (ms-python.mypy-type-checker) - строгая проверка типов при сохранении
- **Black Formatter** (ms-python.black-formatter) - автоформатирование при Ctrl+S

При !линт используй read_lints() для проверки через IDE, затем CLI для детальной диагностики
Ошибки показываются в Problems (Ctrl+Shift+M) автоматически при открытии/сохранении файлов

---
🧹 Pylint:
При написании кода сразу обращай внимание и не допускай следующих ошибок:
🔴 Высокий приоритет (критические ошибки):
W0707 - Отсутствует raise ... from e → Добавляй from e для правильной цепочки исключений
W0107 - Недостижимый код (pass statements) → Убирай лишние pass в классах исключений
R1705 - Недостижимый elif после return → Заменяй elif на if после return
R1720 - Недостижимый elif после raise → Заменяй elif на if после raise
no-redef - Дублированные методы → Переименовывай дублированные методы
🟡 Средний приоритет (предупреждения):
W0611 - Неиспользуемые импорты → Удаляй неиспользуемые импорты
W0612 - Неиспользуемая переменная → Убирай неиспользуемые переменные
W1203 - f-строки в logging функциях → Используй lazy % formatting в logging
C0411 - Неправильный порядок импортов → Сначала стандартные библиотеки, затем сторонние
🟢 Низкий приоритет (стиль кода):
C0301 - Длинные строки → Разбивай длинные строки (максимум 79 символов)
C0303 - Trailing whitespace → Убирай лишние пробелы в конце строк
C0325 - Лишние скобки после not → Убирай лишние скобки в условиях if not
W0221 - Неправильные сигнатуры методов → Исправляй сигнатуры методов
📋 Дополнительные правила:
R0917 - Слишком много позиционных аргументов (7-9 вместо 5) → Используй keyword arguments
R0801 - Дублированный код между файлами → Выноси общую функциональность в отдельные модули

---
🔧 MyPy:
Устанавливай types-* пакеты для внешних библиотек: pip install types-requests types-urllib3
Для kwargs используй TypedDict или конкретные типы вместо Any
Unreachable code в функциях валидации с ранним возвратом - это нормально, можно игнорировать
Добавляй импорты from typing import Optional, Any в файлы где используются эти типы
Для Union типов используй hasattr() для проверки наличия атрибута перед обращением к нему
Добавляй аннотации типов **kwargs: Any для всех функций и методов с kwargs
Добавляй -> None для функций без возвращаемого значения и правильные типы для параметров

---
🧪 Pytest:
- Всегда устанавливай pytest в .venv: `pip install pytest pytest-cov`
- Всегда проверяй реальные имена классов и функций в исходном коде перед импортом в тестах, используй grep или read_file для проверки
- Перед тестами проверь сигнатуру: `python -c "from module import Class; print(Class.method.__code__.co_varnames)"`
- Для достижения высокого покрытия кода, фокусируйся на тестировании простых функций и модулей, избегай сложных интеграционных тестов
- Перед созданием тестов для модулей, проверь какие функции реально существуют в исходном коде
- При удалении проблемных тестовых файлов, убедись что не нарушаешь общую структуру тестирования проекта
- При тестировании обработки ошибок, используй только те исключения, которые реально определены в модуле exceptions
- Для достижения максимального покрытия кода, создавай простые тесты для каждого модуля отдельно, а не пытайся покрыть все сразу

---
📝 ДОКУМЕНТАЦИЯ И КОММИТЫ:
🚨 КРИТИЧЕСКИ ВАЖНО - НИКОГДА НЕ ЗАБЫВАЙ:
После КАЖДОГО исправления/дополнения/изменения ОБЯЗАТЕЛЬНО:
1. Обновляй CHANGELOG.md с описанием изменений НА РУССКОМ ЯЗЫКЕ
2. Делай локальный коммит НА РУССКОМ ЯЗЫКЕ: `git add -A && git commit -m "feat/fix/docs: [краткое описание на русском]"`
3. Поддерживай документацию в актуальном состоянии

⚠️ НЕ ЗАВЕРШАЙ ЗАДАЧУ БЕЗ ЭТИХ ДЕЙСТВИЙ!
- Любой человек или ИИ должен моментально вникнуть в историю разработки
- Не ленись - документация = качество проекта
- Если забыл - это серьезная ошибка в процессе разработки
- ВСЕ коммиты и CHANGELOG должны быть на русском языке согласно общим правилам проекта

📋 ЧТО ДОБАВЛЯТЬ В CHANGELOG (только публичные изменения):
✅ Новые публичные функции/методы API
✅ Изменения в поведении существующих публичных методов
✅ Исправления багов, влияющих на пользователей
✅ Изменения в зависимостях (новые требования)
✅ Изменения производительности, заметные пользователям
✅ Новые примеры использования в документации
✅ Критические исправления безопасности

🚫 ЧТО НЕ ДОБАВЛЯТЬ В CHANGELOG (внутренние изменения):
❌ Настройка инструментов разработки (LSP, IDE, расширения)
❌ Детальные исправления линтинга (Pylint, MyPy, Black)
❌ Внутренние рефакторинги без изменения API
❌ Обновление .cursorrules, .editorconfig и других конфигов разработчика
❌ Исправления в тестах (если не связано с багом в коде)
❌ Изменения в CI/CD конфигурации
❌ Внутренняя документация процесса разработки
❌ Настройки окружения разработчика
❌ Технические детали типа "R0917", "W0109", "E1126"
❌ Таймауты MCP/LSP и прочие инструменты разработчика

💡 Правило простое: если изменение НЕ влияет на код пользователя библиотеки - НЕ добавляй в CHANGELOG

---
🧩 GitHub:
Не выдумывай ссылки на несуществующие документы в README.md и т.п.
Всегда проверяй существование внешних ссылок перед добавлением в pyproject.toml - несуществующие ссылки портят репутацию проекта
Не выдумывай несуществующие контакты!
Не пиши README.md слишком пафосно но сохраняй профессиональный тон, и никакого джуновского кринджа! типа "логирование вместо print", чтобы питон программисты не засмеяли, но и чтобы джунов не отпугнуть
В changelog не надо включать внутренние обновления влияющие на безопасность утечки данных разработчика (меня), это не нужная информация для публичных пользователей, так же не нужно добавлять данные о внутренней документации не касающейся публичных функций
Документируй только публичные функции без того, что не касается конечных пользователей программы (внутренняя документация, внутренние функции для разработчика, процесс разработки и т.д.)

---
⚙️ Окружение:
Windows 11 24H2 64 Russian
AMD Ryzen 9 7940HS, RAM 32 ГБ, SSD 1024 ГБ

---
🚨 КРИТИЧЕСКИ ВАЖНО:
Проверка .venv
**ПЕРЕД НАЧАЛОМ ЛЮБОЙ РАБОТЫ ОБЯЗАТЕЛЬНО:**
1. Проверить активирован ли .venv: `python -c "import sys; print('Virtual env:', sys.prefix)"`
2. Если НЕ активирован - создать и активировать: `python -m venv .venv && .venv\Scripts\Activate.ps1`
3. Установить зависимости: `pip install -e . && pip install pytest pytest-cov`

💻 PowerShell на Windows - Особенности
- НЕ используй `mkdir core google yandex` - создавай по одной: `mkdir core && mkdir google`
- Всегда проверяй текущую директорию: `pwd` перед `cd` командами
- Используй абсолютные пути если сомневаешься: `cd C:\full\path\to\directory`

---
🔄 ВЕРСИОНИРОВАНИЕ:
**ПРИ СОЗДАНИИ ЛЮБОГО ПРОЕКТА СРАЗУ ЗАКЛАДЫВАЙ СИСТЕМУ ВЕРСИОНИРОВАНИЯ:**
- `__version__ = "1.0.0"` в `__init__.py`
- Синхронизировать в `pyproject.toml`, `setup.py`
- `CHANGELOG.md` с историей изменений [Keep a Changelog]
- `update_version.py` - скрипт обновления версии (обновляет все файлы)
- `create_release.py` - скрипт релиза (тесты + git tag)
- GitHub Actions для автоматических релизов (триггер: push tags) - собирает пакет и создает GitHub Release
- PyPI upload выполняется автоматически через twine при команде !релиз
- README: инструкции по установке и обновлению

---
🚀 АВТОМАТИЧЕСКИЙ РЕЛИЗ (!релиз):
**ПОЛНЫЙ ЦИКЛ РЕЛИЗА ВЫПОЛНЯЕТСЯ АВТОМАТИЧЕСКИ:**

1. **Определение версии**: Анализируй CHANGELOG.md и определяй следующую версию (patch/minor/major)
2. **Обновление версии**: Запускай update_version.py для синхронизации версии во всех файлах
3. **Коммит изменений**: git add -A && git commit -m "release: bump version to X.X.X"
4. **Создание тега**: git tag -a vX.X.X -m "Release vX.X.X: [описание]"
5. **Push**: git push origin main && git push origin vX.X.X
6. **Сборка пакета**: python -m build (в директории проекта)
7. **PyPI upload**: twine upload dist/xmlriver_pro-X.X.X* (автоматически)
8. **Верификация**: Проверь GitHub Release и PyPI страницу

**ВАЖНО**: Все этапы выполняются автоматически, пользователь только дает команду !релиз
